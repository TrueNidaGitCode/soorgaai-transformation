# SoorgaAI Knowledge Base — Python Tool Dependencies
#
# Core loader (loader.py) — no external dependencies, stdlib only.
#
# Strategy Response Engine (strategy_response_engine.py)
anthropic>=0.40.0

# Vector store + hybrid retrieval
chromadb>=0.5.0

# Embedding Engine (embedding_engine.py) — install the provider you need:

# OpenAI embeddings
langchain-openai>=0.1.0

# HuggingFace embeddings (local, no API key required)
# langchain-community>=0.2.0
# sentence-transformers>=2.7.0

# Ollama embeddings (requires a running Ollama server)
# langchain-community>=0.2.0
