# SoorgaAI — Railway Backend Configuration
[build]
builder = "NIXPACKS"
buildCommand = "cd backend/trunida-backend && npm install"

[deploy]
startCommand = "cd backend/trunida-backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

